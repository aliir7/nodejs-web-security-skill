# GitHub Copilot Security Instructions

Follow repository-root SKILL.md. Do not trust client-supplied ownership, concatenate user input into SQL, pass raw request objects as NoSQL filters, confuse CORS with authentication, assume HttpOnly alone solves CSRF, expose secrets to browser code, or skip server-side authorization.