# Gemini Adapter

Use repository-root SKILL.md for Node.js web security. Treat every client input as untrusted, keep authentication/authorization/ownership distinct, enforce ownership in data access, and verify security properties with tests.